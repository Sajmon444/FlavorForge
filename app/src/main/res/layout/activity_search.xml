<?xml version="1.0" encoding="utf-8"?>
<androidx.drawerlayout.widget.DrawerLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/drawerLayout"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- GŁÓWNY EKRAN -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:background="@color/background"
        android:padding="24dp"
        android:gravity="center_horizontal">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:layout_marginBottom="24dp"
            android:gravity="center_vertical">

            <ImageView
                android:id="@+id/btnMenu"
                android:layout_width="48dp"
                android:layout_height="48dp"
                android:src="@drawable/list_icon"
                android:padding="8dp"
                android:contentDescription="@string/menu_title" />

            <!-- ZMIENIONE: EditText → AutoCompleteTextView -->
            <AutoCompleteTextView
                android:id="@+id/etIngredient"
                android:layout_width="0dp"
                android:layout_height="67dp"
                android:layout_weight="1"
                android:hint="@string/ingredient_hint"
                android:textColorHint="@color/light_grey"
                android:textColor="@color/light_grey"
                android:backgroundTint="@color/light_grey"
                android:imeOptions="actionDone"
                android:inputType="text"
                android:maxLines="1"
                android:paddingStart="12dp"
                android:paddingEnd="12dp"
                android:textSize="14sp" />

            <Button
                android:id="@+id/btnAdd"
                android:layout_width="wrap_content"
                android:layout_height="67dp"
                android:layout_marginStart="8dp"
                android:paddingStart="16dp"
                android:paddingEnd="16dp"
                android:text="@string/add_button_text"
                android:textSize="14sp"
                style="@style/AppButton_orange" />
        </LinearLayout>

        <ImageButton
            android:id="@+id/btnFilters"
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:src="@drawable/filter"
            android:background="@android:color/transparent"
            android:padding="4dp"
            android:tint="@color/light_grey"
            android:layout_gravity="end"
            android:contentDescription="@string/filters_button_text" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/added_ingredients_text"
            android:textStyle="bold"
            android:textSize="16sp"
            android:textColor="@color/light_grey"
            android:layout_marginBottom="12dp" />

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/rvIngredients"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1"
            android:clipToPadding="false"
            android:paddingBottom="8dp" />

        <!-- Suwak dla brakujących składników -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:layout_marginTop="16dp"
            android:layout_marginBottom="8dp"
            android:paddingHorizontal="16dp">

            <TextView
                android:id="@+id/tvMaxMissingLabel"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="Dozwolone brakujące składniki: 2"
                style="@style/AppTextNormal_light_grey"
                android:textSize="14sp"
                android:gravity="center"
                android:layout_marginBottom="8dp" />

            <SeekBar
                android:id="@+id/sbMaxMissing"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:max="5"
                android:progress="2"
                android:thumbTint="@color/orange"
                android:progressTint="@color/orange"
                android:progressBackgroundTint="@color/light_grey" />
        </LinearLayout>

        <Button
            android:id="@+id/btnSearch"
            android:layout_width="289dp"
            android:layout_height="67dp"
            android:text="@string/search_button_text"
            android:textSize="14sp"
            android:enabled="false"
            style="@style/AppButton_orange"
            android:layout_gravity="center_horizontal"
            android:layout_marginTop="8dp" />

    </LinearLayout>

    <!-- LEWY DRAWER (MENU) -->
    <LinearLayout
        android:layout_width="300dp"
        android:layout_height="match_parent"
        android:layout_gravity="start">

        <include layout="@layout/drawer_menu" />
    </LinearLayout>

    <!-- PRAWY DRAWER (FILTRY) -->
    <FrameLayout
        android:layout_width="300dp"
        android:layout_height="match_parent"
        android:layout_gravity="end">

        <include layout="@layout/drawer_filters" />
    </FrameLayout>

</androidx.drawerlayout.widget.DrawerLayout>
